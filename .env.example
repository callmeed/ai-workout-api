# Copy this file to `.env` and fill in the values.

# OpenAI API key
OPENAI_API_KEY=sk-your-key-here

# Port for local Node adapter (optional; defaults to 3000)
PORT=3000

# App environment (e.g. "development", "production")
APP_ENV=development